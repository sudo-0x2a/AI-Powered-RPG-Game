// Systems module (skeletons for future extraction)

export class InputManager {
    constructor(scene) {
        this.scene = scene;
    }
}

export class DialogueManager {
    constructor(scene) {
        this.scene = scene;
    }
}

export class InventoryManager {
    constructor(scene) {
        this.scene = scene;
    }
}

export class QuestSystem {
    constructor(scene) {
        this.scene = scene;
    }
}


